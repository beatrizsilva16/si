import numpy as np
import pandas as pd

class VarianceThreshold:
    def __int__(self, threshlod):
        self.threshold = threshlod
        self.variance = None #está vazio porque não vamos calcular variance aqui

    def __init__(self, ):
        variance = np.var(dataset)
        self.variance = variance

    return # o fit returna ele próprio

    def transform (dataset, ) # vai retornar um novo dataset
        X = dataset.X

        mask =
        novoX = dataset.X[:,mask]


if __main__ == '__main__':
    from si data dataset import Dataset
    dataset = Dataset (X: np.array([[0, 2, 0, 1]
                                    [0, 1, 4, 3]]
                                    [0, 1, 1, 3]])



